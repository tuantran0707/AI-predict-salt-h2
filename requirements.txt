# For dev machine (Windows / Linux x86_64) and Raspberry Pi 4 (aarch64).
# On Raspberry Pi OS Bookworm 64-bit, prefer the apt packages for camera/OpenCV:
#     sudo apt install -y python3-picamera2 python3-opencv
# then create the venv with --system-site-packages so picamera2 is available.

numpy>=1.23
opencv-python>=4.8           # On Pi: skip this and rely on apt python3-opencv
tensorflow>=2.13 ; platform_machine == "x86_64"
# On Raspberry Pi 4 (aarch64) install one of:
#   pip install tensorflow-aarch64
#   pip install tflite-runtime         # lighter, requires using a .tflite model
