# Works the same on Windows / Linux x86_64 and Raspberry Pi 4 (aarch64).
#
# Raspberry Pi 4 (Bookworm or Trixie):
#   sudo apt install -y python3-picamera2 python3-opencv python3-numpy
# Then create the venv with --system-site-packages OR just run with system python3.
# No TensorFlow needed - inference runs through OpenCV DNN on the bundled
# MobileNetV2 ONNX model in model/.

numpy>=1.23
opencv-python>=4.8       # On Pi: skip and rely on apt python3-opencv
